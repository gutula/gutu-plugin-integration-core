# Edge Cases

- Block connectors whose hosts fall outside the allowlist.
- Degrade health when schema cache expires even if the connection is otherwise valid.
- Preserve denied-tool posture even when allowlists are also present.
